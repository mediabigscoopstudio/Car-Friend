asgiref==3.11.1
channels==4.3.2
daphne==4.2.1
Django==5.2.15
django-hosts==7.0.0
djangorestframework==3.17.1
pillow==12.2.0
psycopg-binary==3.3.4
psycopg2-binary==2.9.12
sqlparse==0.5.5
reportlab
opencv-python-headless
requests
weasyprint
python-dotenv
# Plate masking uses opencv-python-headless + pillow (above) — works out of the
# box. ultralytics/YOLO is OPTIONAL and only helps with a PLATE-SPECIFIC model
# (set settings.PLATE_YOLO_MODEL); stock yolov8n.pt has no 'plate' class and it
# pulls torch (~2GB). Install only if you add a real plate model:
# ultralytics
